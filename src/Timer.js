import React from 'react';

export const Timer = () => {
  return (
    <div>
      <h2>TODO: Current time</h2>
      <button>Start</button>
      <button>Stop</button>
      <button>Reset</button>
    </div>
  );
};